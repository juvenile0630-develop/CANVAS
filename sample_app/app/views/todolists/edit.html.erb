<h1>edit</h1>
<%= form_for(@list, url:"/todolists/#{@list.id}") do |f| %>
  <h4>title</h4>
  <%= f.text_field :title %>
  <h4>content</h4>
  <%= f.text_area :body %>
  <%= f.submit 'edit' %>    
<% end %>

